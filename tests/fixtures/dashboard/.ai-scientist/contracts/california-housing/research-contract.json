{
  "research_contract": {
    "contract_id": "california-housing",
    "title": "Lower RMSE on California housing on the fixed split",
    "goal": "Lower RMSE on California housing on the fixed split",
    "dataset": {
      "name": "california-housing",
      "version": "1.0"
    },
    "split_protocol": "official train/test split, 10% of train held out for validation, seed 13",
    "primary_metric": "rmse",
    "primary_metric_direction": "lower_is_better",
    "success_threshold": 0.48,
    "evaluator_command": "python eval.py --split test"
  }
}
