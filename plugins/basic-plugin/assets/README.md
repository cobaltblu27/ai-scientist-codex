# Assets

Place plugin icons, logos, screenshots, and other static assets here.

Update the `interface.composerIcon`, `interface.logo`, and `interface.screenshots` values in `.codex-plugin/plugin.json` when real assets are added.
